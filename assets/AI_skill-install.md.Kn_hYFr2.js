import{_ as a,o as l,c as e,ag as i}from"./chunks/framework.C9sp7nFj.js";const k=JSON.parse('{"title":"npx skills add 是怎么安装 Skill 的","description":"","frontmatter":{},"headers":[],"relativePath":"AI/skill-install.md","filePath":"AI/skill-install.md"}'),t={name:"AI/skill-install.md"};function n(d,s,p,o,c,r){return l(),e("div",null,[...s[0]||(s[0]=[i(`<h1 id="npx-skills-add-是怎么安装-skill-的" tabindex="-1"><code>npx skills add</code> 是怎么安装 Skill 的 <a class="header-anchor" href="#npx-skills-add-是怎么安装-skill-的" aria-label="Permalink to &quot;\`npx skills add\` 是怎么安装 Skill 的&quot;">​</a></h1><p>社区里常见这种安装方式：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> skills</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> freestylefly/awesome-gpt-image-2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --global</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --all</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --copy</span></span></code></pre></div><p>它<strong>不是</strong>把 Skill 装进 <code>node_modules</code>，也不会起一个插件进程。电脑实际做的事就 7 步：<code>npx</code> 从 npm 下载安装程序 → 程序去 GitHub clone 仓库 → 找到 <code>SKILL.md</code> → 拷到 Cursor 等会扫描的目录。逐步拆解见下面「实际调用链」。</p><p>Skill 是什么、装完之后怎么进上下文，见：<a href="./skill.html">Skill 说明</a>、<a href="./skill-how.html">Skill 生效原理</a>。本文只讲安装器本身。</p><p>上面这条来自 <a href="https://github.com/freestylefly/awesome-gpt-image-2" target="_blank" rel="noreferrer">awesome-gpt-image-2</a> 的 README，含义是「装到本机所有支持的 Agent」。</p><h2 id="先拆命令" tabindex="-1">先拆命令 <a class="header-anchor" href="#先拆命令" aria-label="Permalink to &quot;先拆命令&quot;">​</a></h2><table tabindex="0"><thead><tr><th>片段</th><th>作用</th></tr></thead><tbody><tr><td><code>npx skills</code></td><td>不全局安装 CLI，临时跑 npm 包 <code>skills</code></td></tr><tr><td><code>add owner/repo</code></td><td>GitHub 简写，等价于 <code>https://github.com/freestylefly/awesome-gpt-image-2</code></td></tr><tr><td><code>--global</code> / <code>-g</code></td><td>装到用户目录，所有项目都能用</td></tr><tr><td><code>--all</code></td><td>仓库里<strong>所有</strong> Skill → 本机<strong>所有已检测到的</strong> Agent，并跳过交互</td></tr><tr><td><code>--copy</code></td><td>每个 Agent 目录写一份<strong>真实文件</strong>，不建符号链接</td></tr></tbody></table><p>只给 Cursor 装、且只要这一个技能，可以写成：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> skills</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> freestylefly/awesome-gpt-image-2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --skill</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gpt-image-2-style-library</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --agent</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cursor</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --global</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --yes</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --copy</span></span></code></pre></div><p><code>add</code> 还接受完整 GitHub / GitLab URL、<code>git@...</code>、本地路径、直接指向某个 <code>SKILL.md</code> 的 tree 地址。</p><h2 id="实际调用链-这条命令电脑在干什么" tabindex="-1">实际调用链（这条命令电脑在干什么） <a class="header-anchor" href="#实际调用链-这条命令电脑在干什么" aria-label="Permalink to &quot;实际调用链（这条命令电脑在干什么）&quot;">​</a></h2><p>就两样东西：</p><table tabindex="0"><thead><tr><th>名字</th><th>人话</th></tr></thead><tbody><tr><td><strong><code>npx</code></strong></td><td>Node 自带的「临时跑一个 npm 包」工具。你电脑上可以没有安装过 <code>skills</code>，它会先去网上把包下载下来再跑。</td></tr><tr><td><strong><code>skills</code> 包</strong></td><td>Vercel 放到 npm 上的安装程序。包名就叫 <code>skills</code>，源码在 <a href="https://github.com/vercel-labs/skills" target="_blank" rel="noreferrer">vercel-labs/skills</a>。</td></tr></tbody></table><p>这条命令电脑实际走的步骤：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. 你敲：</span></span>
<span class="line"><span>   npx skills add freestylefly/awesome-gpt-image-2 --global --all --copy</span></span>
<span class="line"><span></span></span>
<span class="line"><span>2. npx 去 npm 官网（registry.npmjs.org）找一个名叫 skills 的包</span></span>
<span class="line"><span>   下载下来，执行里面的程序：bin/cli.mjs</span></span>
<span class="line"><span></span></span>
<span class="line"><span>3. 这个程序读你后面的字：</span></span>
<span class="line"><span>   add     → 要安装</span></span>
<span class="line"><span>   freestylefly/awesome-gpt-image-2  → GitHub 用户/仓库名</span></span>
<span class="line"><span>   --global --all --copy             → 装哪、装几个、怎么拷</span></span>
<span class="line"><span></span></span>
<span class="line"><span>4. 程序把 owner/repo 拼成 GitHub 地址：</span></span>
<span class="line"><span>   https://github.com/freestylefly/awesome-gpt-image-2</span></span>
<span class="line"><span>   然后 git clone 整仓下来（拷到一个临时文件夹）</span></span>
<span class="line"><span></span></span>
<span class="line"><span>5. 在仓库里找带 SKILL.md 的文件夹</span></span>
<span class="line"><span>   这个仓找到的是：</span></span>
<span class="line"><span>   agents/skills/gpt-image-2-style-library/</span></span>
<span class="line"><span></span></span>
<span class="line"><span>6. 按参数把这个文件夹写到本机：</span></span>
<span class="line"><span>   --global → 写到你家目录，不是当前项目</span></span>
<span class="line"><span>   --all    → Cursor / Claude / Codex……本机有哪个就装哪个</span></span>
<span class="line"><span>   --copy   → 真拷文件，不建快捷方式（符号链接）</span></span>
<span class="line"><span></span></span>
<span class="line"><span>7. 装完了。Cursor 下次开会话，去 ~/.cursor/skills/ 扫到这份 SKILL.md</span></span></code></pre></div><p>画成图：</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart TD</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  A[&quot;1 你敲 npx skills add owner/repo&quot;] --&gt; B[&quot;2 npx 从 npm 下载 skills 这个包&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  B --&gt; C[&quot;3 跑包里的 bin/cli.mjs&lt;br/&gt;解析 add / 仓库名 / 参数&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  C --&gt; D[&quot;4 git clone&lt;br/&gt;github.com/freestylefly/awesome-gpt-image-2&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  D --&gt; E[&quot;5 找到 SKILL.md 所在文件夹&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  E --&gt; F[&quot;6 拷到 ~/.cursor/skills/ 等目录&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  F --&gt; G[&quot;7 Cursor 启动时扫描这些目录&quot;]</span></span></code></pre></div><p>对应到源码也就是两步：<code>npx</code> 启动 <code>cli.ts</code> → <code>add</code> 子命令里 <code>cloneRepo()</code>，再 <code>discoverSkills()</code>。</p><h2 id="原理-写一次-分发到各-host" tabindex="-1">原理：写一次，分发到各 Host <a class="header-anchor" href="#原理-写一次-分发到各-host" aria-label="Permalink to &quot;原理：写一次，分发到各 Host&quot;">​</a></h2><p>各产品扫的路径不一样：Cursor 看 <code>~/.cursor/skills/</code>，Claude Code 看 <code>~/.claude/skills/</code>，Codex 看 <code>~/.codex/skills/</code>……Skill 本身只是带 YAML 头的 <code>SKILL.md</code>（<a href="https://agentskills.io" target="_blank" rel="noreferrer">Agent Skills 规范</a>）。</p><p><code>npx skills add</code> 做的是：<strong>clone 仓库 → 发现 Skill → 按产品目录落地</strong>。</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart LR</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  A[&quot;npx skills add owner/repo&quot;] --&gt; B[git clone / 下载]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  B --&gt; C[&quot;扫描 SKILL.md&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  C --&gt; D{--global?}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  D --&gt;|是| E[用户目录]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  D --&gt;|否| F[当前项目]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  E --&gt; G{--copy?}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  F --&gt; G</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  G --&gt;|默认 symlink| H[&quot;先拷到规范目录&lt;br/&gt;~/.agents/skills/name/&lt;br/&gt;再 ln 到各 Agent&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  G --&gt;|--copy| I[直接拷到每个 Agent 目录]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  I --&gt; J[&quot;Cursor: ~/.cursor/skills/&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  I --&gt; K[&quot;Claude: ~/.claude/skills/&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  I --&gt; L[&quot;Codex: ~/.codex/skills/&quot;]</span></span></code></pre></div><p>对这个仓库，发现路径是现成的：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>agents/skills/gpt-image-2-style-library/SKILL.md</span></span></code></pre></div><p>CLI 会在仓库里找这些位置（浅层递归，最多约 3 层）：根目录、<code>skills/</code>、<code>.agents/skills/</code>、<code>.claude/skills/</code>，以及 <code>.claude-plugin/marketplace.json</code> 声明的路径。找不到再全库搜。这个项目还带 Claude Code 插件清单，所以 <code>npx skills add</code> 和 <code>/plugin marketplace add</code> 都能找到同一份 Skill。</p><p>检测「本机装了哪些 Agent」很朴素：看有没有对应配置目录。例如存在 <code>~/.cursor</code> 就算 Cursor 已安装。</p><h2 id="三个参数各自解决什么" tabindex="-1">三个参数各自解决什么 <a class="header-anchor" href="#三个参数各自解决什么" aria-label="Permalink to &quot;三个参数各自解决什么&quot;">​</a></h2><h3 id="global-跨项目-不污染当前仓库" tabindex="-1"><code>--global</code>：跨项目，不污染当前仓库 <a class="header-anchor" href="#global-跨项目-不污染当前仓库" aria-label="Permalink to &quot;\`--global\`：跨项目，不污染当前仓库&quot;">​</a></h3><table tabindex="0"><thead><tr><th>范围</th><th>标志</th><th>Cursor 落地</th></tr></thead><tbody><tr><td>项目（默认）</td><td>无</td><td><code>./.agents/skills/</code>（可进 git，给团队）</td></tr><tr><td>本机全局</td><td><code>-g</code></td><td><code>~/.cursor/skills/</code></td></tr></tbody></table><p>风格库、出图协议这类能力跟具体业务仓库无关，适合全局。装完后任意项目开 Cursor 都能扫到。</p><p>规范目录（canonical）在全局时是 <code>~/.agents/skills/</code>。Cursor 全局路径是 <code>~/.cursor/skills/</code>，项目路径走通用的 <code>.agents/skills/</code>。</p><h3 id="all-跳过「选哪个-skill、装给哪个-agent」" tabindex="-1"><code>--all</code>：跳过「选哪个 Skill、装给哪个 Agent」 <a class="header-anchor" href="#all-跳过「选哪个-skill、装给哪个-agent」" aria-label="Permalink to &quot;\`--all\`：跳过「选哪个 Skill、装给哪个 Agent」&quot;">​</a></h3><p><code>--all</code> 约等于 <code>--skill &#39;*&#39; --agent &#39;*&#39; -y</code>。</p><p>不加时会交互：选 Skill、选 Agent（或只装本机检测到的）。<code>--all</code> 表示：仓库里发现的全装，本机有 <code>~/.cursor</code>、<code>~/.claude</code>、<code>~/.codex</code> 等就全写一份。</p><p>这个仓库目前主要是一个技能：<code>gpt-image-2-style-library</code>。<code>--all</code> 的价值更在「多 Agent」，不在「很多 Skill」。</p><h3 id="copy-每份独立文件-不依赖-symlink" tabindex="-1"><code>--copy</code>：每份独立文件，不依赖 symlink <a class="header-anchor" href="#copy-每份独立文件-不依赖-symlink" aria-label="Permalink to &quot;\`--copy\`：每份独立文件，不依赖 symlink&quot;">​</a></h3><p>默认（推荐、好更新）是：</p><ol><li>拷一份到规范目录：<code>~/.agents/skills/gpt-image-2-style-library/</code></li><li>在 <code>~/.cursor/skills/...</code>、<code>~/.claude/skills/...</code> 建<strong>相对路径符号链接</strong>指向那一份</li><li>symlink 失败再退回 copy</li></ol><p><code>--copy</code> 跳过规范目录那一步，<strong>直接把目录复制到每个 Agent 路径</strong>。好处：</p><ul><li>Windows / 部分沙箱对 symlink 不友好</li><li>有的 Host 不跟随符号链接</li><li>改某一端的 Skill 不会改到另一端</li></ul><p>代价：多份拷贝，<code>npx skills update</code> 要各写一遍，磁盘也更大。这个 Skill 还带 <code>references/</code>、<code>assets/</code>、<code>bin/</code>，copy 会整目录复制。</p><p>仓库推荐 <code>--copy</code>，是为了「装完就能用、少踩 symlink」。只给自己用、且更新频繁，可以去掉 <code>--copy</code>，用默认 symlink。</p><h2 id="装完之后怎么生效" tabindex="-1">装完之后怎么生效 <a class="header-anchor" href="#装完之后怎么生效" aria-label="Permalink to &quot;装完之后怎么生效&quot;">​</a></h2><p>安装只是把文件放到磁盘。Cursor / Claude Code 启动时扫描约定目录，<strong>不会</strong>把整份 <code>SKILL.md</code> 立刻塞进模型。</p><ol><li><strong>L1</strong>：只注入 <code>name</code> + <code>description</code>（每个大约一百来个 token）</li><li><strong>L2</strong>：用户说「用风格库出一张信息图」或打 <code>/gpt-image-2-style-library</code> 时，才读完整 <code>SKILL.md</code></li><li><strong>L3</strong>：正文里点到 <code>references/</code> 再打开，点到 <code>bin/</code> 再执行</li></ol><p>所以：装了 ≠ 每条消息都带着整份案例库。<code>description</code> 写清楚，Agent 才会去打开它。装完后<strong>新开一轮会话</strong>更稳妥。完整加载机制见：<a href="./skill-how.html">Skill 生效原理</a>。</p><h2 id="和「npm-装包」的差别" tabindex="-1">和「npm 装包」的差别 <a class="header-anchor" href="#和「npm-装包」的差别" aria-label="Permalink to &quot;和「npm 装包」的差别&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th><code>npx skills add</code></th><th><code>npm i -g gpt-image-2-style-library</code></th></tr></thead><tbody><tr><td>产物</td><td>各 Agent 的 <code>skills/</code> 目录</td><td>全局 npm 包 + 该包自己的 <code>install</code></td></tr><tr><td>消费者</td><td>Cursor / Claude / Codex …</td><td>还要再跑 <code>gpt-image-2-style-library install all</code></td></tr><tr><td>本质</td><td>分发 Markdown 协议</td><td>分发 Node CLI</td></tr></tbody></table><p>同一仓库两条路：<code>npx skills add</code> 是跨 Agent 的标准分发；npm 包是他们自己的同步脚本，主要写 <code>~/.codex/skills</code>、<code>~/.claude/skills</code>、<code>~/.agents/skills</code>。</p><h2 id="装完怎么查、怎么更新" tabindex="-1">装完怎么查、怎么更新 <a class="header-anchor" href="#装完怎么查、怎么更新" aria-label="Permalink to &quot;装完怎么查、怎么更新&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> skills</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -g</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # 看全局装了什么</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> skills</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -a</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cursor</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> skills</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -g</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # 拉仓库最新再覆盖</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> skills</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> remove</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --global</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gpt-image-2-style-library</span></span></code></pre></div><p>Cursor 侧应能看到：<code>~/.cursor/skills/gpt-image-2-style-library/SKILL.md</code>（<code>--copy</code> 时是实体目录）。</p><h2 id="一句话" tabindex="-1">一句话 <a class="header-anchor" href="#一句话" aria-label="Permalink to &quot;一句话&quot;">​</a></h2><p><strong><code>npx</code> 负责跑安装器，<code>owner/repo</code> 负责定位源，<code>--global</code> 决定写到家目录，<code>--all</code> 决定全 Skill × 全 Agent 且不提问，<code>--copy</code> 决定用复制而不是链接。</strong> 装完之后仍是普通 Skill：被扫描到、被 <code>description</code> 选中、再读进上下文。</p>`,55)])])}const g=a(t,[["render",n]]);export{k as __pageData,g as default};
